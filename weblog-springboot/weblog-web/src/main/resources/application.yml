spring:
  profiles:
    # 默认激活 dev 环境
    active: dev
# 应用服务 WEB 访问端口

#=================================================================
# log 日志
#=================================================================
logging:
  config: classpath:logback-weblog.xml
